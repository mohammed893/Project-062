const request = require('supertest');
const app = require("../../app");

describe('Test Archive', () => {
    test("sub test 1 : ", () => {
        res = 100;
        expect(res).toBe(100);
    });
})